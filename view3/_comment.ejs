<!-- <link rel="stylesheet" href="/css/postcomment.css"> -->

<li id="comment-<%= comment._id %>"><p>
    <!-- so that if post owner want to delete inappropriate comment he/she can -->
    <% if((locals.user)&&((locals.user.id==comment.user.id))) {%>
    <small>
        <a class="delete-comment-button" href="/comments/destroy/<%= comment.id%>"> delete</a>
    </small>
    <%}%>
    <%= comment.content%>
    <br>
    <small>
        <%= comment.user.name%>
    </small>
</p>
</li>